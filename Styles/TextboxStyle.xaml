<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Note_Keeper">
    <Style x:Key="TextboxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{DynamicResource TextboxNoFocusColor}"/>
        <Setter Property="CaretBrush" Value="{Binding RelativeSource={x:Static RelativeSource.Self},Path=Foreground}"/>
        <Setter Property="SelectionBrush" Value="#09f"/>
        <Setter Property="SelectionOpacity" Value="0.2"/>
        <Setter Property="FontSize" Value="16"/>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="true">
                <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            </Trigger>
            
        </Style.Triggers>
    </Style>
    
</ResourceDictionary>